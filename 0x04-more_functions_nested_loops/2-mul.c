#include<stdio.h>
#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: is the first digit
 * @b: is the second digit
 * Return: 0
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
